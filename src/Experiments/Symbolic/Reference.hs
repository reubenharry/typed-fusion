{-# LANGUAGE DataKinds #-}

-- | Former flat-buffer oracles for coalesced sector spines.
--
-- Genealogy 'Rep' / 'RepV' is now the production fused representation; densify
-- via tree payloads + CG when needed. This module is intentionally empty until
-- tree-facing oracles are rewritten.
module Experiments.Symbolic.Reference () where
